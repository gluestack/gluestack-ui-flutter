import 'package:flutter/material.dart';

class GSAvatarBadge extends StatelessWidget {
  const GSAvatarBadge({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}