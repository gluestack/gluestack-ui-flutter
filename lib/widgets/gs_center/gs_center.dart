import 'package:flutter/material.dart';

class GSCenter extends Align {
  /// Creates a widget that centers its child.
  const GSCenter({ super.key, super.widthFactor, super.heightFactor, super.child });
}