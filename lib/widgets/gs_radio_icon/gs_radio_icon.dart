import 'package:flutter/material.dart';

class GSRadioIcon extends StatelessWidget {
  const GSRadioIcon({super.key});

  @override
  Widget build(BuildContext context) {
    return Radio(value: '1', groupValue: '2', onChanged: (c) {});
  }
}
